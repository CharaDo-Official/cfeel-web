# Webサイト更新マニュアル for Cursor

ようこそ！このプロジェクトは **React (Vite)** と **Tailwind CSS** で作られています。
技術的な細かいことがわからなくても、**Cursor (AIエディタ)** を使えば会話するように修正できます。

このマニュアルでは、「どこを触れば何が変わるか」と「AIへの指示の出し方（プロンプト）」を中心に解説します。

---

## 📁 フォルダ構成：どこに何がある？

編集作業の9割は `src` フォルダの中で完結します。

### 1. ページの中身を変えたいとき (`src/pages/`)
画面ごとのファイルです。「文章を変えたい」「画像を差し替えたい」ときはここを見ます。

| フォルダ/ファイル | 対応するページ | 備考 |
| :--- | :--- | :--- |
| **charado/** | **CharaDo製品関連** | 一番よく触る場所です |
| ├ `CharaDoHome.tsx` | CharaDoトップ | 製品紹介、機能一覧など |
| ├ `Character.tsx` | キャラクター紹介 | 紬やノアの紹介文、動画設定 |
| ├ `Plan.tsx` | プラン一覧 | （現在は準備中） |
| ├ `Policy.tsx` | ポリシー | CharaDoポリシーの文章 |
| ├ `Law.tsx` | 特商法表記 | 法律関連の堅い文書 |
| └ `Terms.tsx` | 利用規約 | 規約関連の文書 |
| **aboutus/** | **会社・組織情報** | |
| ├ `Home.tsx` | About Usトップ | ミッション、バリュー（C+FEEL） |
| └ `Privacy.tsx` | プライバシーポリシー | サイト全体のプライバシー規定 |
| **(ルート直下)** | | |
| ├ `Home.tsx` | サイトトップ | CFEEL全体のトップページ |
| └ `Support.tsx` | サポート | お問い合わせ先メールアドレス |

### 2. ヘッダー・フッター・メニューを変えたいとき
| ファイル | 場所 | 何ができる？ |
| :--- | :--- | :--- |
| `src/components/layout/Header.tsx` | ヘッダー | ロゴ、上部のメニューバー |
| `src/components/layout/Footer.tsx` | フッター | ページ最下部のリンク集、コピーライト |
| `src/constants/navigation.ts` | **★メニュー設定** | **メニューの項目名やリンク先はここで一括管理しています** |

### 3. 画像・動画を追加したいとき
`src/assets/` に入れます。
- `characters/` : キャラクターの動画や画像
- ロゴ画像などもここにあります

---

## 🤖 Cursor（AI）への指示のコツ

新任のあなたにとって、最強の味方は **Cursor** です。
`Ctrl + L` (Windows) / `Cmd + L` (Mac) でチャットを開いて、以下のように頼んでみましょう。

### 基本ルール
1. **ファイルを見せる**: 修正したいファイルを開くか、チャットで `@ファイル名` と打って参照させます。
2. **具体的に言う**: 「いい感じにして」よりも「文字を大きくして」「青色にして」の方が伝わります。

### シチュエーション別プロンプト例

#### ケース1：文章を直したい
> **あなた**: `@CharaDoHome.tsx このページの「新感覚デスクトップマスコット」という文章を、「あなたに寄り添うAIパートナー」に変更して。`

#### ケース2：デザインを変えたい（具体的）
> **あなた**: `@Character.tsx キャラクターの名前の色を、今は黒だけど、もっと目立つピンク色（#ff0080）に変えてほしい。`

#### ケース3：デザインを変えたい（雰囲気で）
> **あなた**: `@CharaDoHome.tsx 今のデザインは少し寂しいので、もっとポップで明るい雰囲気にデザインし直して。アニメの公式サイトみたいな感じで、角丸を多く使ってほしい。`

#### ケース4：新しいセクションを追加したい
> **あなた**: `@CharaDoHome.tsx 一番下に「ユーザーの声」というセクションを追加して。3人のユーザーの感想を横並びで表示したい。`

#### ケース5：メニューを増やしたい
> **あなた**: `@navigation.ts メニューに「ニュース」を追加して。リンク先は /news で。`

---

## 🌳 Git（バージョン管理）のルール【超重要！】

ここだけは必ず守ってください！

### 1. 作業を始めるときは「ブランチ」を切る
**絶対に `main` ブランチのまま作業しないでください！**
作業ごとに、自分の作業場所（ブランチ）を作ります。

**ブランチ名のルール**: `/feat/あなたの名前/やったこと`
例: `/feat/yamada/change-top-text`

**Cursorでのやり方**:
1. 左下のブランチ名（`main` とか書いてあるところ）をクリック
2. 「Create new branch...」を選択
3. `feat/yamada/update-header` のように入力してEnter

### 2. こまめに「コミット」する
「コミット」＝「セーブ」です。
1つの変更（例：テキストを変えた、色を変えた）が終わるたびにコミットしましょう。

**ルール**:
- **動作確認してから！**: 画面が真っ白になっていないか確認してからコミットします。
- **リファクタリングも**: AIにコードを修正（リファクタリング）してもらったら、その都度コミットします。

### 3. マージ（合流）は管理者がやります
作業が終わったら報告してください。`main` への合流は管理者が行います。

---

## ⚠️ 注意点（これだけは気をつけて！）

1. **`App.tsx` は慎重に**:
   - ここは「サイト全体の地図（ルーティング）」を管理しています。ページを新しく作ったとき以外はあまり触らなくて大丈夫です。
   - AIに任せるときも「ルーティングを変えて」と言わない限り、勝手に壊れることは少ないですが、念のため。

2. **画像・動画のパス**:
   - 新しい画像を入れたら、ファイルパス（`src/assets/...`）を正確にAIに伝えましょう。
   - 例: `src/assets/new_image.png を追加したので、これを使って`

3. **困ったときは「元に戻して」**:
   - AIが変な修正をして画面が真っ白になっても焦らないでください。
   - `Ctrl + Z` で戻るか、AIに「さっきの変更を取り消して」と言えば直してくれます。

---

頑張ってください！Cursorがいれば、コードが書けなくても立派なWebサイト管理者です！

